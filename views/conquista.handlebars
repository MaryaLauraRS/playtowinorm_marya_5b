<h1>Conquistas Cadastrados</h1>

<a href="/jogos/{{jogos.id}}/conquistas">cadastrar novo Conquista</a>

<table border="1">

    <tr>
    <td>titulo</td>
    <td>descrição</td>
    </tr>
    {{#each Conquista}}
    <tr>
    <td>{{this.titulo}}</td>
    <td>{{this.descricao}}</td>
    </tr>
    {{/each}}
</table>